const name = require('./name')
const password = require('./password')
const category = require('./category')
const slot = require('./slot')

const scenes = [name, password, category, slot]

module.exports = {
  scenes
}
